import React from "react";
import { useSelector } from "react-redux";

export const  DataErrorAlert=()=>{
// 	const dataError = useSelector(state => state.fetchRedusers.dataError)
// 	const data = useSelector(state => state.fetchRedusers.data)
	
// 	let classes=['bg-light']
		
// 		if(dataError !== ''/*data.Response==='False'*/){
// 		classes.splice(0,1,'alert-warning')
// 		// setTimeout(()=>classes.splice(0,1,'bg-light'),3000)
// 	}else{classes.splice(0,1,'bg-light')}
// console.log(dataError)
// 	return(
// 	<div className={`${classes.join()} alert`} role="alert">
// 		{dataError}
// 	</div>
// 	)
}